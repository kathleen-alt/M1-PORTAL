{
  "name": "m1-portal",
  "private": true,
  "version": "1.0.0",
  "description": "Market One — Content Intelligence Portal. Sources real, linked financial/business news, segments into content pillars, runs AI analysis, generates on-brand copy + branded graphics, and runs an approval → publish workflow.",
  "type": "module",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --workspace server run dev",
    "dev:client": "npm --workspace client run dev",
    "build": "npm --workspace client run build",
    "start": "npm --workspace server start",
    "lint": "echo \"(no linter configured)\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
